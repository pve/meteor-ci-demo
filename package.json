{
  "name": "workspace",
  "private": true,
  "scripts": {
    "start": "meteor run"
    "test": "meteor test --once --driver-package dispatch:mocha"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "meteor-node-stubs": "~0.2.4"
  }
}
