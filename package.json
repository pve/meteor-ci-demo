{
  "name": "workspace",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "TEST_BROWSER_DRIVER=phantomjs meteor test --once --full-app --driver-package dispatch:mocha"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "core-js": "^2.4.1",
    "meteor-node-stubs": "~0.2.4"
  },
  "devDependencies": {
    "phantomjs-prebuilt": "^2.1.14"
  }
}
